# Project status

Last updated: 2026-09-09 (end of the first session, run from the parent directory).

## What exists and works

- **Repo:** https://github.com/larsbrubaker/music-editor (public, `main`, 3 commits). Local: `~/Development/music-editor`.
- **Toolchain:** Node 26 and Bun installed via Homebrew at `/opt/homebrew/bin` (may need to be on PATH). `bun dev` serves the site with live reload on port 3000; `npm test` runs the headless suite.
- **Tests:** 38 tests in `tests/`, all passing in ~60 ms (`node --test tests/`), covering the graphics helpers, recognition, reactions/undo, the whole music model driven by real gestures, and the 800-line file limit.
- **Ported code (all of the course):**
  - `src/graphics/`: `G` (V, VS, LoHi, BBox, PL, HC, Transform, splines), `Graphics` canvas shim, `WinApp` harness + `Timer`, `Color`, `UC`.
  - `src/reaction/`: `Ink` (+Norm, Buffer), `Shape` (+Prototype, Database with localStorage + JSON, Trainer), `Reaction` (+List, Map), `Layer`, `Mass`, `Gesture` (+undo by replay), `World.reset()`.
  - `src/music/`: `Page`, `Sys`, `Staff`, `Bar`, `Time`, `Duration`, `Rest`, `Head`, `Stem`, `Beam`, `Clef`, `Key`, `Glyph`, `MusicEd`. This is the course's MusicEd version (Page/HC based) with the later fixes folded in (removeHead/deleteStem, Sys beam reaction, guards).
  - `src/sandbox/`: RedRect, PaintShapes, Paint, Squares1, Squares2, SquaresGame, Squares (areas), PaintInk, ShapeTrainer, ReactionTest, GlyphSheet, SplineDemo.
- **Shipped shape database** `assets/shapes.json` (generated by `node scripts/gen-shapes.js` through the real pipeline): N-N, S-S, E-E, W-W, W-E, SW-SW, W-S, E-S, SW-SE, SE-SW, O (+DOT is special-cased).
- **Site shell:** `index.html` (landing + chapter list), `app/` (the editor with gesture legend, undo/new page/train buttons, shapes.json download/load/reset), `site/` (css, nav, demo runner, source viewer).
- **Verified in the browser** (dev server, Chromium pane): the editor loads the font and shapes; a real mouse drag is recognized as W-W and draws the first staff; a scripted sequence produced staffs, a second system, G/F clefs propagating to the next system, single/double/fine bars, repeat dots, heads, stems, flags, quarter and eighth rests.

## Deviations from the course, on purpose (all commented in code)

- `HC.v()` sums offsets down to ZERO (the course text's version returns 0 at the page top).
- `Ink.Buffer` resamples strokes by **arc length** by default because pointer events on a fast flick deliver very few samples; the course's index sub-sampling is kept (`Ink.Buffer.arcLength = false`, PaintInk toggle).
- Reactions take `bid`/`act` as arrow functions instead of anonymous subclasses with `Box.this`.
- Array subclasses declare `Symbol.species`; `Mass` needs no `equals/hashCode` fix in JS.

## Known issues / next steps, in order

1. **Accidental glyph codes are wrong.** `Glyph.SHARP/FLAT/NATURAL` in `src/music/Glyph.js` are guesses (61475/61538/61550); 61475 renders as a curly stroke, not a sharp. Run the `GlyphSheet` demo (`src/sandbox/GlyphSheet.js`, mount it on any page or add it to a lesson) and pick the right codes and dy offsets, then check a key signature in the app. Also confirm the hot spots of REST_W/REST_H, the flags and HEAD_W visually.
2. **Beam creation in the browser:** the same scripted sequence creates a beam in Node (`/tmp` replay showed the Sys E-E reaction winning with bid 50) but the browser screenshot showed a flag on the first stem instead. Likely a one-pixel rounding difference in `WinApp.toMouseEvent` moving a head one line; verify with a real stroke and, if it is real, write the failing test first.
3. **Lesson pages do not exist yet.** The nav links to `lessons/01-warm-up.html` … `08-refactoring-and-appendix.html` (see `site/nav.js` CHAPTERS). Each page should: summarize the course sections in our own words (link to the original text rather than copying it), show the real source via `<div class="code" data-src="src/...">`, and embed demos via `<div class="demo" data-app="Squares2" data-toggles="showSpline:Show spline">` (see `site/demo.js`, `site/code.js`, `site/lesson.js`). Suggested demos per chapter: 1 RedRect/PaintShapes/Paint; 2 Squares1/Squares2/SquaresGame/Squares; 3 none (narrative); 4 PaintInk/ShapeTrainer; 5 ReactionTest; 6 MusicEd; 7 GlyphSheet/MusicEd; 8 SplineDemo.
4. **Publish:** enable GitHub Pages from `main` / root (`gh api -X POST repos/larsbrubaker/music-editor/pages -f build_type=legacy -f source[branch]=main -f source[path]=/`), verify https://larsbrubaker.github.io/music-editor/ and the font loads there.
5. **CI:** add `.github/workflows/test.yml` running `node --test tests/` on push (Node 22+).
6. **Review pass** with the `reviewer` agent (`.claude/agents/reviewer.md`) over `src/reaction` and `src/music` before calling it done.
7. Nice to have: mobile/touch check, `Glyph.debugBoxes` toggle in the app, an "O" gesture use, README screenshot.

## How the pieces fit (for orientation)

Mouse → `WinApp` → `Gesture.AREA` → `Ink.BUFFER` → `new Ink()` (Norm + VS) → `Shape.recognize` (nearest prototype, or DOT) → `Gesture` → `Reaction.best` (every enabled reaction for that shape bids; lowest wins) → `act` → new/changed `Mass` in a `Layer` → `paintComponent` shows `Layer.ALL`. Undo = pop the gesture list, `Layer.nuke()` + `Reaction.nuke()`, replay.
